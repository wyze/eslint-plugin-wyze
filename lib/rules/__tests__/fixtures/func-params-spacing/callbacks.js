
const a = b.reduce(( c, d) => c + d, 0)
const a = b.reduce((c, d ) => c + d, 0)
const a = b.reduce((c, d) => c + d, 0)
const a = b.reduce(( ) => Math.random(5), 0)
const a = b.reduce(function (c, d) { c + d }, 0)
